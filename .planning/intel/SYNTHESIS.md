# Synthesis Summary — trasker doc ingest

Mode: new (net-new bootstrap, no existing `.planning/` context to reconcile against)
Precedence: ADR > SPEC > PRD > DOC (default)
Generated by: gsd-doc-synthesizer

---

## Doc counts by type

- ADR: 0
- SPEC: 2
- PRD: 0
- DOC: 8
- Total: 10

All 10 classifications had `confidence: high` and `manifest_override: true`. No UNKNOWN-low-confidence docs.

---

## Decisions locked

0 — there are no ADRs in the ingest set, so by definition no `locked: true` decisions. The architectural / technology choices in the SPECs are recorded as **constraints** (SPEC-level authority) and as non-locked "directional choices" in `decisions.md`. None of them are immovable until promoted to a formal Accepted ADR.

Source SPECs (where the directional choices originate):
- docs/superpowers/specs/2026-03-23-trasker-design.md
- docs/superpowers/specs/2026-03-26-production-deployment-design.md

---

## Requirements extracted

18 derived requirements (REQ-*) — extracted from SPEC narrative since no formal PRDs were ingested. Treat these as PRD-shaped extracts pending formal authoring. None are competing variants because none have explicit acceptance-criteria language.

IDs:
- REQ-focus-tracking
- REQ-presence-detection
- REQ-tagging-system
- REQ-note-cascade
- REQ-submit-flow
- REQ-pomodoro
- REQ-system-tray
- REQ-autostart
- REQ-first-run
- REQ-local-dashboard
- REQ-server-dashboard
- REQ-rbac
- REQ-api-key-lifecycle
- REQ-client-binary-build-pipeline
- REQ-offline-resilience
- REQ-cross-platform-support
- REQ-production-deployment-ux
- REQ-admin-bootstrap

---

## Constraints

22 constraint entries by type:

- nfr: 8 (CONSTRAINT-tech-stack, -repository-layout, -timezone-convention, -security, -error-handling, -production-architecture, -spa-embedding, -ci-pipeline, -out-of-scope-v1)
- protocol: 7 (CONSTRAINT-platform-support-matrix, -presence-state-machine, -note-cascade-algorithm, -api-key-lifecycle, -autocert-listeners, -client-binary-distribution, -jwt-secret-precedence, -admin-bootstrap-matrix)
- api-contract: 2 (CONSTRAINT-api-endpoints, -submission-payload)
- schema: 2 (CONSTRAINT-client-sqlite-schema, -server-postgres-schema)

(Counts above use grouped categories; some entries straddle categories — see file for canonical type tags.)

---

## Context topics

8 — one per DOC input:

- Build sequence and parallelization (plan 00)
- Shared foundation (plan 01)
- Server core (plan 02)
- Client core (plan 03)
- Server dashboard & deployment (plan 04) — partially superseded
- Client UI & features (plan 05)
- Cross-platform & integration (plan 06)
- Production deployment plan (plan 26)

Plus a synthesizer-authored "Architectural pivot to note" topic that flags the 03-23 → 03-26 deployment-architecture revision for downstream planners.

---

## Conflicts

- BLOCKERs: 0
- WARNINGs: 1 — two SPECs address the same scope (production deployment topology) with divergent technical content; the newer SPEC self-declares as a replacement but neither is `locked`, so a human must confirm precedence before routing
- INFO: 4 — no-ADR note, no-PRD note, cross-ref-graph-acyclic note, and Plan 04 partial-supersession note

Full report: `/home/jaypaulb/Projects/gh/trasker/.planning/INGEST-CONFLICTS.md`

---

## Per-type intel files

- `/home/jaypaulb/Projects/gh/trasker/.planning/intel/decisions.md`
- `/home/jaypaulb/Projects/gh/trasker/.planning/intel/requirements.md`
- `/home/jaypaulb/Projects/gh/trasker/.planning/intel/constraints.md`
- `/home/jaypaulb/Projects/gh/trasker/.planning/intel/context.md`

---

## Status

AWAITING USER — one WARNING (the SPEC-vs-SPEC deployment-topology overlap) requires explicit user resolution before `gsd-roadmapper` should be routed. Recommended resolutions in order of preference:

1. Author a locked ADR recording the 4-container → 2-container pivot, citing the 2026-03-26 SPEC as the authoritative deployment design.
2. Mark the deployment sections of the 2026-03-23 SPEC as Superseded.
3. Override per-doc precedence via `--manifest` so the 2026-03-26 SPEC outranks the 2026-03-23 SPEC (lowest-friction option but leaves the older SPEC ambiguous for future readers).
